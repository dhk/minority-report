Task
Survey the ad hoc eval harnesses already built in wingman, praxis, and
work-ledger; identify what they have in common and where they diverge.

Context
No shared harness exists today. Each repo built its own under time pressure.

Constraints
Survey only — do not propose a specific shared design in this pass.

Output needs
A comparison table plus a list of open questions a future design pass would
need to resolve.
